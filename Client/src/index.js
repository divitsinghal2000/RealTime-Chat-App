import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
//All work is done in App
//then it is placed inside the root div in HTML


ReactDOM.render(<App />, document.querySelector('#root'));
